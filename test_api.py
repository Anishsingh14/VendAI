import requests
import json
import os

token = "NOT_NEEDED_FOR_TESTING_LOCALLY_IF_WE_USE_DB" # Actually we need to login to get token
# Instead let's just make a dummy request or check the flask server output
